{
  "name": "apollo-mcp-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:api": "node src/server.mjs",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "@apollo/server": "^5.2.0",
    "graphql": "^16.11.0"
  }
}
